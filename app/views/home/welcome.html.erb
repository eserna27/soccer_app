<div class="container-fluid">
  </br>
  <div class="page-header">
    <h4>
      Jornada <%= @week.week_number %>
      <small>
        Liga Méxicana
      </small>
    </h4>
  </div>
  <div class="row">
    <div class="col-sm-11 col-sm-offset-1">
      <table class="table table-hover">
        <tbody>
          <% @week.matches.each do |match| %>
            <%= link_to match_path(match.id) do %>
              <tr>
                <td style="width: 10%;">
                  <%= image_tag "logos/#{match.home_team.logo}" ,class: "team-media-image" %>
                </td>
                <td style="width: 10%;">
                  <h3>vs</h3>
                </td>
                <td style="width: 10%;">
                  <%= image_tag "logos/#{match.away_team.logo}" ,class: "team-media-image" %>
                </td>
                <td>
                  <%= match.date %>
                </td>
              </tr>
            <% end %>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
